#include "function_pointers.h"

/**
 * print_name - a function that prints a name
 * @name: the name to be printed
 * @f: the function to be recalled
*/

void print_name(char *name, void (*f)(char *))
{
	void print(char *)
	{
		printf(char *);
	}

	int main()
	{
		void (*f)(char *);
			f = print;
			f(char *);
	}
}
